# fillmap-apps(埋め立て系アプリ量産工場)

埋め立て系アプリ(地図×塗り)を量産するモノレポ。共通エンジンを使い回し、テーマとデータの差し替えで新作を増やす。

## 構成
- `packages/core`(@fillmap/core バレル): 共通エンジン(保存/達成率/称号判定/シェア画像/塗り状態)
- `apps/<appId>`: 各アプリ。`from "@fillmap/core"` の1本でエンジンを参照
- `docs/`: 戦略マスター(00) + シリーズ設計(10/20/30) + NOTELOG仕様(41)
- アプリ別仕様は `apps/<appId>/docs/SPEC.md`

## core抽出方針(重要)
- 現状 packages/core は railmap から「鉄道固有込みで素直に昇格」した段階。
- **真の汎用/固有の線引きは、2本目を実コードで並べた時に行う**(早すぎる抽象化を避ける。00_master §7-2)。
- 新アプリ追加時、core の鉄道固有処理(距離換算/路線種別集計等)はアプリ側へ剥がす。

## 厳守(00_master §5)
- 唯一の仕様書はアプリの `docs/SPEC.md`。仕様にない実装・ライブラリ追加・デザイン変更は禁止。曖昧なら実装前に質問。
- 実データ・実設定値を確認してからコードを書く(推測禁止)。
- localStorage/IndexedDB のみ・外部送信なし(地図タイル/計測PVを除く)・保存データに必ず `version`。
- 全コメント/コミュニケーションは日本語。回答は変更点のみ簡潔に。

## NOTELOGルール(必須・docs/41_notelog_spec.md §3形式)
タスク完了ごとに `docs/NOTELOG.md` へ1エントリ追記(モノレポ1冊集約・アプリ名列で区別)。
- 詰まり・エラー・想定外が発生したら、解決した時点で必ず記録(エラーメッセージは原文1行引用)
- 順調だったタスクも「やったこと」1行は残す
- 記録は事実のみ・各項目1行・テンプレ厳守。感想や冗長な経緯は書かない
- スクショが記事映えする瞬間(初回描画成功・ビフォーアフター等)には 📷 を付ける

## タスク完了時
- アプリの `docs/PROGRESS.md` に「完了 / 決定 / 残課題」を3行以内で追記。
